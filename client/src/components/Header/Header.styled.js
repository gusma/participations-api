import styled from 'styled-components';

export const HeaderContainer = styled.div.attrs(() => ({
  className: 'mb-5 w-full',
}))``;

export const NavBarContainer = styled.div.attrs(() => ({
  className:
    'flex w-full text-left p-5 text-white font-bold bg-gradient-to-r from-black via-violet-500 to-violet-500 background-animate',
}))``;

export const IconContainer = styled.div.attrs(() => ({
  className: 'mr-5',
}))``;
