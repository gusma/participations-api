import styled from 'styled-components';

export const TRContainer = styled.tr.attrs(() => ({
  className: 'transition duration-300 ease-in-out hover:bg-gray-100',
}))``;

export const TDContainer = styled.td.attrs(() => ({
  className: 'text-sm text-gray-900 font-medium px-6 py-4',
}))``;
